/*global describe:true, it:true, before:true, after:true */

var
	chai       = require('chai'),
	assert     = chai.assert,
	expect     = chai.expect,
	should     = chai.should()
	;

var LocalNode = require('../lib/localnode');

describe('LocalNode', function()
{
	it('has tests for the constructor requirements');
	it('has tests that demonstrate it respects its options');
	it('has tests that demonstrate it returns promises');
	it('has tests that verify the level db it creates');
	it('has TTL tests');


});
